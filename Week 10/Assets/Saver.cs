/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using SimpleJSON;

public class Saver : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GameObject[] As = GameObject.FindGameObjectsWithTag ("A");
		Vector3 pos = As [0].transform.position;
		Vector3 scale = As [0].transform.localScale;

		/*JSONClass json = new JSONClass ();
		json ["xPos"].AsFloat = pos.x;
		json ["yPos"].AsFloat = pos.y;

		AData ad = new AData (pos, scale);
		ad.Save ("a.txt");

		AData loadAd = new AData ("a.txt");
		print (loadAd.position);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}*/
